version: '3.8'

services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: 123456
      POSTGRES_DB: test_project

    ports:
      - 5430:5432
  test-app:
    build: .
    command: ./app
    ports:
      - 8080:8080
    depends_on:
      - db
